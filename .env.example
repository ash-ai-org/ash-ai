# Ash API key (Required for production — protects all API endpoints)
# Generate with: openssl rand -hex 32
# If unset, auth is disabled (local dev mode).
ASH_API_KEY=

# Anthropic API (Required for agent execution)
ANTHROPIC_API_KEY=sk-ant-...

# npm publishing (Required for `make publish`)
# Get a token at https://www.npmjs.com/settings/<user>/tokens
NPM_TOKEN=npm_...

# S3 Files (agent and project file storage)
S3_FILES_BUCKET=ash-session-files
S3_FILES_REGION=us-east-1
S3_FILES_ACCESS_KEY_ID=
S3_FILES_SECRET_ACCESS_KEY=

# AWS Credentials (Required for EC2 deployment: ./scripts/deploy-ec2.sh)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1

# EC2 Deployment (see docs/guides/ec2-deployment.md)
EC2_KEY_NAME=                    # Name of your EC2 key pair
EC2_KEY_PATH=~/.ssh/my-key.pem  # Path to the private key file
EC2_INSTANCE_TYPE=t3.large      # Instance type (2 vCPU, 8GB RAM)
EC2_VOLUME_SIZE=30              # Root volume size in GB
EC2_SECURITY_GROUP_ID=          # Optional: use existing SG (auto-created if empty)
EC2_SUBNET_ID=                  # Optional: deploy to a specific subnet

# GCP Credentials (Required for GCE deployment: ./scripts/deploy-gce.sh)
# Authentication is via `gcloud auth login` — no keys needed in .env
GCP_PROJECT_ID=                  # GCP project ID (or set via `gcloud config set project`)
GCP_ZONE=us-east1-b             # Compute Engine zone
GCP_MACHINE_TYPE=e2-standard-2  # Machine type (2 vCPU, 8GB RAM)
GCP_DISK_SIZE=30                # Boot disk size in GB (SSD)

# Distributed mode (coordinator + runner on separate machines)
ASH_INTERNAL_SECRET=             # Auto-generated if empty (used for server↔runner auth)
ASH_RUNNER_PORT=4200             # Port the runner listens on

# Cloud workspace persistence (optional — enables durable cross-machine session resume)
# Supported: s3://bucket/prefix/ or gs://bucket/prefix/
# If empty, persistence is local-only.
ASH_SNAPSHOT_URL=
# Override S3 region (default: us-east-1, or from AWS_DEFAULT_REGION)
ASH_S3_REGION=us-east-1
